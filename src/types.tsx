export interface UserProps {
    user_name: string;
    full_name: string;
    balance: number;
}

export interface ProductProps {
    id: string;
    name: string;
    quantity: number;
    price: number;
}
