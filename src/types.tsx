export interface UserProps {
    id: string | null;
    user_name: string | null;
    balance: number | null;
}

export interface ProductProps {
    id: string;
    name: string;
    quantity: number;
    price: number;
}
